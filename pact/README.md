# Pact Contract Tests

Pact contract files are generated by `tests/MySubscriptionService.ContractTests/`
and published to PactFlow by the `pact-publish` GitHub Actions job.

- Consumer tests generate a contract from the consumer's perspective
- Provider tests verify this service honors contracts others expect
- Contracts are tagged `pull-request` on PR and `main` on main branch push
